type Booking @model @auth(rules: [{ allow: private }]) {
  id: ID!
  customerName: String!
  phoneNumber: String!
  carBrand: String!
  carYear: String!
  licensePlate: String!
  mileage: Int!
  selectedItems: String!
  totalPrice: Float!
  bookingDate: AWSDate!
  bookingTime: AWSTime!
  status: String
  mechanicName: String # 🟢 ต้องมีบรรทัดนี้ เพื่อเก็บชื่อช่าง
}

type Part @model @auth(rules: [{ allow: public }, { allow: private }]) {
  id: ID!
  categoryKey: String!
  categoryName: String!
  name: String!
  price: Int!
  isFixed: Boolean!
  stock: Int # 🟢 ต้องมีบรรทัดนี้ เพื่อเก็บสต็อก
}